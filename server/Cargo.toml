[package]
name = "anu-server"
version = "0.1.0"
edition = "2021"
description = "Anu serving half: Candle inference + Axum OpenAI-compatible API"

[dependencies]
tokio = { version = "1", features = ["full"] }

[dev-dependencies]

[profile.release]
opt-level = 3
lto = "thin"